# Netlify AI Gateway injects provider keys automatically after the first
# production deploy. Do not set OPENAI_API_KEY / ANTHROPIC_API_KEY / GEMINI_API_KEY
# yourself — that bypasses the gateway.
#
# Optional local overrides (only if testing without gateway):
# OPENAI_BASE_URL=
# OPENAI_API_KEY=
